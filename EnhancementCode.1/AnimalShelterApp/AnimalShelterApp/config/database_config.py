#Database configuration for MongoDB connectivity
#This is being moved to a local host from the original AAC
#host, since the database is no longer accessible

MONGO_URI = "mongodb://localhost:27017"
DATABASE_NAME = "animal_shelter"
COLLECTION_NAME = "animals"